services:
  auth-service:
    build:
      context: ..
      dockerfile: services/auth/Dockerfile
    restart: unless-stopped
    container_name: tasker-auth-service
    env_file:
      - ../.env
      - ../services/auth/.env
    depends_on:
      - postgres
      - redis
    ports:
      - "50001:50001"